import { Text, View, StyleSheet, Button, Linking } from "react-native";

export const styles = StyleSheet.create({
    linkButton: {
      color: 'blue',
      fontWeight: 'bold',
    },
    map: {
        width: '100%',
        height: '100%',
      },
  });